import { Helmet } from "react-helmet-async";

const Instructors = () => {
    return (
        <div>
            <Helmet>
                <title>Academy | Instructor</title>
            </Helmet>
            <h2>this is instructor page</h2>
        </div>
    );
};

export default Instructors;